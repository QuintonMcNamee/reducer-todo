import React, { useReducer } from 'react'

const initialState = {
  item: 'Learn about reducers',
  completed: false,
  id: 3892987589
}

const reducer = () => {
  return default;
}

export default (initialState, reducer);
